# Backend Configuration

# ============================================================================
# Database Configuration
# ============================================================================
# URL de conexão do banco de dados PostgreSQL
# Formato: postgresql://usuario:senha@host:porta/database
# 
# Opções:
# 1. Docker local: postgresql://docildos:docildos_dev@localhost:5432/docildos_db
# 2. Supabase: postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres
# 3. Railway: postgresql://postgres:[PASSWORD]@[HOST]:5432/railway
# 4. Neon: postgresql://[USER]:[PASSWORD]@[HOST]/[DATABASE]?sslmode=require
DATABASE_URL=postgresql://docildos:docildos_dev@localhost:5432/docildos_db

# ============================================================================
# CORS Configuration
# ============================================================================
# Origens permitidas para CORS (separadas por vírgula)
CORS_ORIGINS=http://localhost:8080,http://localhost:5173

# ============================================================================
# Environment
# ============================================================================
# Ambiente de execução: development, staging, production
ENVIRONMENT=development

# ============================================================================
# LLM Provider (para referência, configuração real está no ai-service)
# ============================================================================
# Provider de LLM a usar: 'ollama' (gratuito) ou 'openai' (pago)
LLM_PROVIDER=ollama
